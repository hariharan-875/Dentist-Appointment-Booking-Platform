PORT=4000
# SQLite DB file (relative or absolute path)
SQLITE_DB_PATH=./db/app.db

# Frontend origin for CORS (change to your deployed frontend URL in production)
FRONTEND_ORIGIN=http://localhost:5173

